---
description: Show MUSCLE token, cache, parser, and command-output savings evidence
---

Show local MUSCLE savings evidence. Execute:

```bash
muscle savings
```

For structured output suitable for automation:

```bash
muscle savings --json
```

Use the JSON form when comparing LLM call tokens, prompt compaction savings,
command-output compaction savings, cache impact, and parser degradation counts.

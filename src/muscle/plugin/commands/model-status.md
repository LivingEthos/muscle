---
description: Show resolved model identity, confidence, and active model-pack overlays
---

Inspect the effective model identity for the current project.
Project-local memory stays primary; model packs only layer on top when identity and policy allow it.

```bash
muscle model status
```

To inspect how identity has changed over recent runs:

```bash
muscle model history
```
